# Copyright (c) 2026 Atum Labs, Inc.
# SPDX-License-Identifier: MIT

# Payer private key (0x-prefixed 32-byte hex).
#
# LEAVE THIS UNSET for a stub run: the payment is signed offline and never touches a
# chain, so the client generates a throwaway key per run and prints its address.
#
# For real settlement (npm run pay), either:
#   export PRIVATE_KEY=0xYourOwnTestnetKey          # shell — wins if both are set
#   PRIVATE_KEY=0xYourOwnTestnetKey                 # this file — uncomment and paste
# Do not write PRIVATE_KEY=$PRIVATE_KEY: dotenv does not expand that, and a placeholder
# here is worse than no value. The funded e2e suite (npm run test:e2e) does not read
# .env — export the key in that shell.
# PRIVATE_KEY=

# Base URL of the MPP-gated resource. The client appends the purchase id, so it requests
# <MERCHANT_URL>/<purchase id> — the purchase is part of the resource, the way an invoice
# or job id already is in most merchant APIs.
# Pair with the sibling mpp-accept-payments example (runs on http://localhost:4030).
MERCHANT_URL=http://localhost:4030/paid

# Source-chain RPC URL (Base Sepolia). Needed only for real settlement, where the
# client approves the source token (Permit2) before paying. Leave blank for the stub —
# there is no real chain to approve on, and a value here makes the stub run contact it.
#   RPC_URL=https://sepolia.base.org
RPC_URL=

# There is deliberately no PURCHASE_ID here. Each run generates and prints a fresh
# purchase id, so every run is a new payment. Pass it on the command line only to
# re-attempt a payment that was interrupted while still settling:
#   PURCHASE_ID=<the id the run printed> npm run pay
# Setting it here instead would make every run re-attempt the same payment, and later runs
# would be served without paying. See "Retries and idempotency" in the README.
