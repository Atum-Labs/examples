# The MPP method package is not on public npm yet, so it resolves from GitHub Packages.
# TODO (before merging to main): delete this file and switch the dependency
#   @atum-labs/mppx-atum-escrow  →  @atumlabs/mppx-atum-escrow
# once the package is published to the public npm registry.
@atum-labs:registry=https://npm.pkg.github.com
